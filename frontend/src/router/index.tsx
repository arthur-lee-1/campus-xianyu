// src/pages/Home/index.tsx
const Home = () => {
  // 先不导入任何组件，只渲染纯文本
  return (
    <div style={{ fontSize: '30px', textAlign: 'center', marginTop: '100px' }}>
      🏠 这是原来的 Home 页面，现在能显示了！
    </div>
  );
};

export default Home;